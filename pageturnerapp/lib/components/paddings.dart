import 'package:flutter/cupertino.dart';

class ProjectPaddings {
  static const smallMainPadding =
      EdgeInsets.symmetric(horizontal: 25, vertical: 10);
  static const verticalSymetric = EdgeInsets.symmetric(vertical: 5);
  static const horizontalMainPadding = EdgeInsets.symmetric(
    horizontal: 20,
  );
  static const mainPadding = EdgeInsets.symmetric(horizontal: 20, vertical: 20);
  static const onlyleftItemsPadding = EdgeInsets.only(left: 20);
  static const onlyrightItemsPadding = EdgeInsets.only(left: 10);
  static const smalltopItemsPadding = EdgeInsets.only(top: 10);
  static const topItemsPadding = EdgeInsets.only(top: 40);
  static const cardPadding =
      EdgeInsets.only(left: 10, right: 20, top: 15, bottom: 15);
  static const miniPadding = EdgeInsets.all(4);
}
